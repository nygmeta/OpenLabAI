# Core: MCP server framework.
# Pinned below 2.0 — the servers use the low-level mcp.server.Server decorator
# API (@app.list_tools / @app.call_tool), which changed in mcp 2.x.
mcp>=1.27.0,<2

# OT-2 and LIMS servers: HTTP client for REST APIs.
httpx>=0.28.0

# Required to run the protocol files that create_protocol generates.
pylabrobot>=0.2.0

# Slack control surface (integrations/slack_bot.py). Optional: the bot runs with
# --dry-run without it, and no other component imports it.
slack-bolt>=1.21.0
slack-sdk>=3.33.0

# Windows only. Required by the Cellario and Biomek FXP servers for COM
# automation; both fall back to mock mode when it is absent.
pywin32>=311; sys_platform == "win32"
